<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.mycompany.myapp.mapper.MemberMapper">

  	<insert id="memberJoin">
  	INSERT INTO userTbl values(#{uid}, #{name}, #{upass}, #{email}, #{address}, now(), 1, 1)
  	</insert>


  	<select id="memberLogin" resultType="com.mycompany.myapp.vo.MemberVO">
  	SELECT uid, upass FROM userTbl WHERE uid=#{uid} AND upass = #{upass}
  	</select>
  </mapper>
  
